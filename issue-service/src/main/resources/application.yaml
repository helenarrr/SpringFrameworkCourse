server:
  port: 8280

spring:
  application:
    name: issue-service

  jpa:
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:postgresql://localhost:5432/app_db
    username: user
    password: passwd

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

